# Contributing

This repo is protected by rulesets. Direct pushes to `main` are not allowed.

## Workflow (always)

1) Start from `main` and create a branch
```bash
git switch main
git pull
git switch -c <type>/<short-scope>
